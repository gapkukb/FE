/* eslint-env node */
const define = require('../../tailwind.config.common.cjs');
///** @type {import('tailwindcss').Config} */
module.exports = define({
  theme: {
    fontSize: {
      sm: '12px',
    },
  },
});
